package com.inolas.solutions.ctci;

import com.inolas.classes.ListNode;
import com.inolas.classes.TreeNode;

/*
* Given a binary tree,
* design an algorithm which creates
* a linked list of all the nodes at each depth
*  (e.g., if you have a tree with depth D,
*  you'll have D linked lists).
* */

public class c4_3_ListOfDepths
{
    public static ListNode listOfDepts(TreeNode bt){
        ListNode head = new ListNode(0);
        return head;
    }
}